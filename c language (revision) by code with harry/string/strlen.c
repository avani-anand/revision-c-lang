#include<stdio.h>
#include<STRING.H>

int main(){
char *st ="harry";
int a= strlen(st);

printf("the length of string st is %d",a);
printf("the length of string st is %d",strlen(st));
return 0 ;
}